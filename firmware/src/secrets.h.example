// Copy this file to src/secrets.h (git-ignored) and fill in your values.
// Only needed for the Wi-Fi TCP transport (M2); the default USB-CDC transport ignores it.
#pragma once

#ifndef MUNINN_WIFI_SSID
#define MUNINN_WIFI_SSID "your-network"
#endif
#ifndef MUNINN_WIFI_PASS
#define MUNINN_WIFI_PASS "your-password"
#endif
#ifndef MUNINN_LISTENER_HOST
#define MUNINN_LISTENER_HOST "192.168.1.50"   // the PC running muninn_listener
#endif
